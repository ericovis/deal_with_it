version: '3'
services:
  web:
    build: .
    command: python3 app.py
    restart: always
    environment:
      BUCKET_NAME: deal_with_it_dev
      S3_ACCESS_KEY: key
      S3_SECRET_KEY: secretKey
      S3_ENDPOINT: s3:4567
    volumes:
      - .:/app
    ports:
      - 5000:5000
