version: '3'
services:
  web:
    build: .
    command: uvicorn src.app:app --reload --host 0.0.0.0 --port 5000
    environment: 
      DEBUG: 1
    volumes:
      - .:/app
    ports:
      - 5000:5000
